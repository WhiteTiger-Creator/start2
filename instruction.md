Act as the manufacturing-execution engineer picking up after a failed batch-platform rollout. It left `/app/data/jobs.json` holding a stale partial instance and left `/app/workflow/scheduler.py` sequencing jobs against draft rules the governance board never approved. Nothing the scheduler emits can be trusted until the shipped work is routed: the released routings, their template library and the shop calendar sit beside that stale file under `/app/data`. Expanding a release into a job's concrete family, processing time, due date and weight — how far a template chain is followed, what displaces what, how an unresolved template counts, and how the set is de-duplicated and ordered — is a governance decision, and the result must land at the path the scheduler reads first.

Then restore the scheduler, preserving its `--input` and `--output-dir` options and their defaults; the setup matrix and the scheduling policy are always read from their fixed absolute paths under `/app/data`, and `--input` selects the jobs-and-precedence instance only. `/app/docs/report_spec.json` is the output contract: paths, schemas, required-field lists, coercions and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/scheduling_governance_log.md`, mostly routine noise, where the later dated decision governs and the board recorded only its departures from ordinary shop practice. One run must finish inside the contract's 240-second wall-clock budget, and writes `schedule.json`, `objective.json` and `summary.json`. Derive every value from the operational inputs: standard library only, no third-party solver or graph library and no dynamic code execution, correct against an alternate instance, identical across reruns, and leave the frozen incident snapshot in `/app/workflow` untouched.
